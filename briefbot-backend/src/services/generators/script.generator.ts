import type { CreativeBrief, ScriptOutline } from '../../types/brief.types.js';

export async function generateScriptOutline(brief: CreativeBrief): Promise<ScriptOutline> {
  void brief; // legacy stub, use BriefGenerator.generateScriptOutline for real usage
  return { scenes: [] };
}
